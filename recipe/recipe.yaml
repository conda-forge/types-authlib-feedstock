schema_version: 1

context:
  python_min: "3.10"
  version: "1.7.2.20260806"

package:
  name: types-authlib
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/t/types-authlib/types_authlib-${{ version }}.tar.gz
  sha256: 4d69dd38b58d0fcfe63b29aad1e1a7b98038291ce183a923554e24f061fe691f

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=82.0.1
    - pip
  run:
    - python >=${{ python_min }}
    - cryptography

tests:
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - pip check
  - package_contents:
      site_packages:
        - authlib-stubs/__init__.pyi

about:
  summary: Typing stubs for Authlib
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/python/typeshed

extra:
  recipe-maintainers:
    - peterbygrave
